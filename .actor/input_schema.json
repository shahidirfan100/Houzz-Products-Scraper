{
  "title": "Houzz Products Scraper",
  "description": "Ultra-fast Houzz product scraper using Constructor.io API. Extracts titles, prices, ratings, reviews, images, and more.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "query": {
      "title": "🔍 Search Query",
      "type": "string",
      "description": "Product to search for on Houzz (e.g., 'sofa', 'dining table', 'outdoor furniture').",
      "editor": "textfield",
      "prefill": "sofa",
      "example": "modern sectional sofa"
    },
    "results_wanted": {
      "title": "📊 Maximum Products",
      "type": "integer",
      "description": "Maximum number of products to collect.",
      "minimum": 1,
      "default": 20,
      "editor": "number"
    },
    "max_pages": {
      "title": "📑 Maximum Pages",
      "type": "integer",
      "description": "Safety limit on the number of API pages to fetch.",
      "minimum": 1,
      "editor": "number",
      "default": 20
    },
    "proxyConfiguration": {
      "title": "🌐 Proxy Configuration",
      "type": "object",
      "description": "Proxy settings. Datacenter proxies are used by default for speed.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true
      }
    }
  },
  "required": [],
  "additionalProperties": false
}