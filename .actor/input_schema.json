{
  "title": "Houzz Products Scraper",
  "description": "Fast Houzz product scraper using Constructor.io API. Extracts titles, prices, ratings, reviews, images, and more.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "query": {
      "title": "🔍 Search Query",
      "type": "string",
      "description": "Product to search for on Houzz (e.g., 'sofa', 'dining table', 'outdoor furniture').",
      "editor": "textfield",
      "prefill": "sofa",
      "example": "modern sectional sofa"
    },
    "results_wanted": {
      "title": "📊 Maximum Products",
      "type": "integer",
      "description": "Maximum number of products to collect. Leave empty or set to a high number to collect all available products.",
      "minimum": 1,
      "default": 20,
      "editor": "number"
    },
    "max_pages": {
      "title": "📑 Maximum Pages",
      "type": "integer",
      "description": "Safety limit on the number of search result pages to visit.",
      "minimum": 1,
      "editor": "number",
      "default": 20
    },
    "proxyConfiguration": {
      "title": "🌐 Proxy Configuration",
      "type": "object",
      "description": "Use Apify Proxy to avoid blocking. Residential proxies are recommended for best results.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      }
    }
  },
  "required": [],
  "additionalProperties": false
}